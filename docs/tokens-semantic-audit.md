# Tokens Semantic Audit

> This file is auto-generated by `scripts/generate-tokens-semantic-audit.mjs`.
> Do not edit manually.

Generated at: 2026-02-26T12:35:35.991Z

Scope:
- Semantic token source: `packages/toolkit/core/settings/_tokens-semantic.scss`
- Runtime blocker scan: `packages/**/*.scss`
- Runtime exclusions: `**/dist/**`, `**/node_modules/**`, semantic source file
- Docs/text scan: `docs/**/*.{md,mdx}`, `packages/site/views/**/*.njk`

## Deletion Gate

`_tokens-semantic.scss` is removable only when all semantic tokens are at least `runtime-cleared` and token replacement regression checks pass.

Status meanings:
- `blocking-runtime`: token still used by runtime SCSS and blocks deletion.
- `runtime-cleared`: no runtime SCSS usage; docs/text may still reference it.
- `fully-cleared`: no runtime SCSS usage and no docs/text references.

## Summary

- Total semantic tokens: 0
- Runtime-used tokens: 0
- Runtime-unused tokens: 0
- Tokens with docs/text references: 0
- Tokens without docs/text references: 0
- All runtime blockers cleared: yes
- All runtime and docs/text references cleared: yes

## Token Matrix

| Token | Maps To | Runtime Usage Count | Runtime Scope | Runtime File References | Docs/Text Ref Count | Docs/Text Files | Status |
|---|---|---:|---|---|---:|---|---|

## Ready-To-Remove Candidates Now (Runtime Cleared)

- none

## Follow-Up Migration Queue

| Token | Runtime Usage Count | Maps To | Runtime Scope |
|---|---:|---|---|

## Refresh

```bash
pnpm run audit:tokens-semantic
```

## When To Re-Run

- After any PR that replaces semantic token references in SCSS.
- Before merging token cleanup/removal PRs.
- Immediately before removing `packages/toolkit/core/settings/_tokens-semantic.scss`.

## Migration Pass Checklist

- Pick highest runtime-usage tokens first.
- Replace semantic aliases with mapped new token equivalents in runtime SCSS.
- Run lint/tests for impacted packages.
- Run `pnpm run audit:tokens-semantic`.
- Confirm token status moved from `blocking-runtime` to `runtime-cleared`.
- Clean docs/text references to avoid consumer/developer confusion.
- When all tokens are runtime-cleared and regression checks pass, remove semantic file + import.

## Deprecation Cleanup Job (Final Removal)

When `_tokens-semantic.scss` is fully removed, also remove deprecation tracking artifacts in the same cleanup pass:

- Remove `packages/toolkit/core/settings/_tokens-semantic.scss`.
- Remove the semantic import from `packages/toolkit/core/settings/_all.scss`.
- Remove this audit document (`docs/tokens-semantic-audit.md`).
- Remove the generator script (`scripts/generate-tokens-semantic-audit.mjs`).
- Remove root command `audit:tokens-semantic` from `package.json`.
- Remove any remaining references to this deprecation workflow in docs.
- Run full validation (`npm test`) before merging cleanup.

## Prompt For Next Pass

Use this prompt with Codex after a migration pass:

```text
Re-run `pnpm run audit:tokens-semantic`, summarize which tokens moved to runtime-cleared,
list any tokens still blocking runtime deletion, and propose the next highest-impact token
replacement slice with affected files. If everything is fully-cleared, propose the
final deprecation cleanup PR that removes the semantic file, audit doc, script, and command.
```

