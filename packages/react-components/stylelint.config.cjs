/**
 * Stylelint configuration for the react-components package
 * Extends the root monorepo configuration
 */
module.exports = {
  extends: '../../stylelint.config.cjs',
  rules: {
    // Package-specific overrides can be added here if needed
  },
};
