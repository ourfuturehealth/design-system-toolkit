{
  "params": [
    {
      "name": "id",
      "type": "string",
      "required": false,
      "description": "This is used for the main component and to compose id attribute for each item."
    },
    {
      "name": "namePrefix",
      "type": "string",
      "required": false,
      "description": "Optional prefix. This is used to prefix each `item.name` using `-`."
    },
    {
      "name": "describedBy",
      "type": "string",
      "required": false,
      "description": "One or more element IDs to add to the fieldset `aria-describedby` attribute."
    },
    {
      "name": "items",
      "type": "array",
      "required": false,
      "description": "An array of input objects with name, value and classes.",
      "params": [
        {
          "name": "id",
          "type": "string",
          "required": false,
          "description": "Item-specific id. If provided, it will be used instead of the generated id."
        },
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "Item-specific name attribute."
        },
        {
          "name": "label",
          "type": "string",
          "required": false,
          "description": "Item-specific label text. If provided, this will be used instead of `name` for item label text."
        },
        {
          "name": "value",
          "type": "string",
          "required": false,
          "description": "If provided, it will be used as the initial value of the input."
        },
        {
          "name": "classes",
          "type": "string",
          "required": false,
          "description": "Classes to add to the generated text input."
        },
        {
          "name": "autocomplete",
          "type": "string",
          "required": false,
          "description": "Autocomplete attribute to identify input purpose, for instance `bday-day`."
        },
        {
          "name": "inputmode",
          "type": "string",
          "required": false,
          "description": "Input mode for the generated text input. Defaults to `numeric`."
        },
        {
          "name": "pattern",
          "type": "string",
          "required": false,
          "description": "Pattern attribute applied to the generated text input. Defaults to `[0-9]*`."
        },
        {
          "name": "attributes",
          "type": "object",
          "required": false,
          "description": "HTML attributes (for example data attributes) to add to the generated input."
        }
      ]
    },
    {
      "name": "hint",
      "type": "object",
      "required": false,
      "description": "Options for the hint component.",
      "isComponent": true
    },
    {
      "name": "errorMessage",
      "type": "object",
      "required": false,
      "description": "Options for the error message component. The error message component will not display if you use a falsy value for `errorMessage`, for example `false` or `null`.",
      "isComponent": true
    },
    {
      "name": "fieldset",
      "type": "object",
      "required": false,
      "description": "Options for the fieldset component (for example legend).",
      "isComponent": true
    },
    {
      "name": "formGroup",
      "type": "object",
      "required": false,
      "description": "Options for the form-group wrapper.",
      "params": [
        {
          "name": "classes",
          "type": "string",
          "required": false,
          "description": "Classes to add to the form-group wrapper."
        }
      ]
    },
    {
      "name": "classes",
      "type": "string",
      "required": false,
      "description": "Classes to add to the date-input container."
    },
    {
      "name": "attributes",
      "type": "object",
      "required": false,
      "description": "HTML attributes (for example data attributes) to add to the date-input container."
    }
  ]
}
